<?xml version="1.0" encoding="utf-8"?>
<!-- Belt and braces alongside allowBackup="false": exclude everything from both
     cloud backup and device-to-device transfer. The sealed identity record and
     any future message database must never leave the device that holds the
     Keystore key protecting them. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
